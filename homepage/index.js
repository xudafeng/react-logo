'use strict';

let React = require('react');
let ReactLogo = require('..');
let ReactDOM = require('react-dom');

let LayoutComponent = require('./layout');

ReactDOM.render(<LayoutComponent><ReactLogo /></LayoutComponent>, document.body);
